import re

def extract_unique_tags(df, col_name="PI Sensors"):
    pattern = r'UN\.ETH\.[A-Za-z0-9]+\.[A-Za-z0-9]+'
    
    all_tags = set()

    for text in df[col_name].dropna():
        matches = re.findall(pattern, str(text))
        all_tags.update(matches)

    return list(all_tags)
