from sklearn.feature_selection import mutual_info_regression

mi = mutual_info_regression(df[X1], df[Y1])

mi_series = pd.Series(mi, index=X1).sort_values(ascending=False)

print(mi_series)
import matplotlib.pyplot as plt

importance.plot(kind='bar')
plt.title("Feature Importance (Impact on Y1)")
plt.show()
